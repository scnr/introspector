require_relative '../webapps/xss_app'

SCNR::Introspector::Configuration.options = {
    application: XssApp,

    coverage:    {
        scope: {
            path_end_with: 'xss_app.rb'
        }
    },

    scanner:     {
        audit: {
            elements: [ :links ]
        },
        checks: ['xss'],
        dom: {
            pool_size: 0
        }
    }
}
