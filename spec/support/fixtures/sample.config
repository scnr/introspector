require_relative '../webapps/xss_app'

Arachni::Introspector::Configuration.options = {
    application: XssApp,

    coverage:    {
        scope: {
            path_ends_with: 'xss_app.rb'
        }
    },

    framework:  {
        audit: {
            elements: [ :links ]
        },
        checks: ['xss'],
        browser_cluster: {
            pool_size: 0
        }
    }
}
